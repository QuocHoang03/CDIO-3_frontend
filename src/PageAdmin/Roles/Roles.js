import React from "react";
import classNames from "classnames/bind";
import styles from "./Roles.module.scss";

const cx = classNames.bind(styles);

const Roles = () => {
  return <div className={cx("wrapper")}>Roles Admin</div>;
};

export default Roles;
